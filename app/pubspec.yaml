name: ai_voice_engine
description: AI Voice Engine - Text to Speech with multiple voices

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.1.1
  audioplayers: ^5.2.1
  path_provider: ^2.1.1
  shared_preferences: ^2.2.2
  flutter_spinkit: ^5.2.1
  dio: ^5.4.0
  download: ^1.0.0
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/
